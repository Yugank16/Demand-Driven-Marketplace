# Add the following lines to the bottom of settings.py file.


try:
   from local_settings import *
except ImportError:
    raise ImportError("A local_settings.py file is required to run this project")
